import { Keys } from "excalibur";

export const UserKeyboardKeybinds = {
    UP: Keys.W,
    RIGHT: Keys.D,
    DOWN: Keys.S,
    LEFT: Keys.A
}

export type KeybindConfig = typeof UserKeyboardKeybinds